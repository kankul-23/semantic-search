# --------------------------------------------------
# FastAPI service config
# --------------------------------------------------

host: 0.0.0.0
port: 8000

reload: true

log_level: info

# --------------------------------------------------
# Default search settings
# --------------------------------------------------

default_top_k: 10

max_query_length: 512

timeout_seconds: 5